import { Table } from '@/features';
import { Layout } from '@/shared/layout/Layout';

export function UsersPage() {
  return (
    <section className="">
      <Layout>
        <Table />
      </Layout>
    </section>
  );
}
